#version 330 core

out vec4 color;

in vec4 result;
void main()
{
    color = vec4(1,0,0,1);
    //color = result.xyz;
    //color = result;
   // color = clamp(color,vec4(0.0,0.0,0.0,0.0), vec4(1.0,1.0,1.0,1.0));
}
