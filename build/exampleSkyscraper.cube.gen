using floor = struct {
	collection positions;
};

floor cfloor(collection positions)
{
	floor tmp;
	tmp.positions = positions;
	return tmp;
}

int init()
{
	collection positions;
	positions.append(cvec3(0.0,1.0,0.0));
	appendSymbol(cfloor(positions));
}
