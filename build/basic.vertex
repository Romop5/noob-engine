#version 330 core

layout(location = 0) in vec3 modelPos;
layout(location = 2) in vec3 vColor;

uniform mat4 mvp;

out vec4 result;
out vec3 vertexcolor;

void main()
{
    vec4 start = mvp*vec4(modelPos,1.0);
    gl_Position = start;
    result = start;
    vertexcolor = vColor;

}
